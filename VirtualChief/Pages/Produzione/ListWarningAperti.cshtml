@page "/Produzione/ListWarningAperti"
@model VirtualChief.Pages.Produzione.ListWarningApertiModel
@{
    ViewData["Title"] = "Warning Aperti";
}

<div class="container">
    <nav aria-label="breadcrumb" class="mt-3 mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="/Produzione/produzione">@Model.NavProduzione</a>
            </li>
            <li class="breadcrumb-item active" aria-current="page">@Model.NavWarningAperti</li>
        </ol>
    </nav>

    <h1>@Model.NavWarningAperti</h1>

    <div class="card">
        <div class="card-body">
            <component type="typeof(Components.Produzione.ListWarningAperti)" 
                       param-Tenant="@Model.Tenant"
                       param-HasPermission="@Model.HasPermission"
                       render-mode="ServerPrerendered" />
             
            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <div class="alert alert-danger mt-3">@Model.ErrorMessage</div>
            }
        </div>
    </div>
</div>
@page "/Produzione/ListRepartiAndon"
@model VirtualChief.Pages.Produzione.ListRepartiAndonModel
@{
    ViewData["Title"] = "Reparti Andon";
}

<div class="container">
    <nav aria-label="breadcrumb" class="mt-3 mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="/Produzione/produzione">@Model.NavProduzione</a>
            </li>
            <li class="breadcrumb-item active" aria-current="page">@Model.NavRepartiAndon</li>
        </ol>
    </nav>

    <h1>@Model.NavRepartiAndon</h1>

    <div class="card">
        <div class="card-body">
            <component type="typeof(Components.Produzione.ListRepartiAndon)" 
                       param-Tenant="@Model.Tenant"
                       param-HasPermission="@Model.HasPermission"
                       param-Reparti="@Model.Reparti" 
                       render-mode="ServerPrerendered" />
             
            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <div class="alert alert-danger mt-3">@Model.ErrorMessage</div>
            }
        </div>
    </div>
</div>
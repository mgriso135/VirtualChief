@page "/Produzione/ArticoliStatoI"
@model VirtualChief.Pages.Produzione.ArticoliStatoIModel
@{
    ViewData["Title"] = "Articoli in Produzione";
}

<div class="container">
    <nav aria-label="breadcrumb" class="mt-3 mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="/Produzione/produzione">@Model.NavProduzione</a>
            </li>
            <li class="breadcrumb-item active" aria-current="page">@Model.NavArticoliStatoI</li>
        </ol>
    </nav>

    <h1>@Model.NavArticoliStatoI</h1>

    <div class="card">
        <div class="card-body">
            <component type="typeof(Components.Produzione.ArticoliStatoI)" 
                       param-Tenant="@Model.Tenant"
                       param-HasPermission="@Model.HasPermission"
                       render-mode="ServerPrerendered" />
             
            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <div class="alert alert-danger mt-3">@Model.ErrorMessage</div>
            }
        </div>
    </div>
</div>
@page "/Produzione/WorkLoadSimReparti"
@model VirtualChief.Pages.Produzione.WorkLoadSimRepartiModel
@{
    ViewData["Title"] = "Simulazione Carico di Lavoro";
}

<div class="container">
    <nav aria-label="breadcrumb" class="mt-3 mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="/Produzione/produzione">@Model.NavProduzione</a>
            </li>
            <li class="breadcrumb-item active" aria-current="page">@Model.NavWorkLoadSimReparti</li>
        </ol>
    </nav>

    <h1>@Model.NavWorkLoadSimReparti</h1>

    <div class="card">
        <div class="card-body">
            <component type="typeof(Components.Produzione.WlListReparti)" 
                       param-Tenant="@Model.Tenant"
                       param-HasPermission="@Model.HasPermission"
                       param-Reparti="@Model.Reparti" 
                       render-mode="ServerPrerendered" />
             
            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <div class="alert alert-danger mt-3">@Model.ErrorMessage</div>
            }
        </div>
    </div>
</div>
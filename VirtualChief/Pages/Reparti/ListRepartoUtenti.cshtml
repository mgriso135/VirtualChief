@page "/Reparti/ListRepartoUtenti"
@model VirtualChief.Pages.Reparti.ListRepartoUtentiModel
@{
    ViewData["Title"] = "Utenti Reparto";
}

<div class="container">
    <nav aria-label="breadcrumb" class="mt-3 mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="/Reparti/listReparti">@Model.NavReparti</a>
            </li>
            <li class="breadcrumb-item active" aria-current="page">@Model.NavListRepartoUtenti</li>
        </ol>
    </nav>

    <h1>@Model.NavListRepartoUtenti</h1>

    <div class="card">
        <div class="card-body">
            <component type="typeof(Components.Reparti.ListRepartoUtenti)" 
                       param-Tenant="@Model.Tenant"
                       param-HasPermission="@Model.HasPermission"
                       param-RepId="@Model.RepId"
                       render-mode="ServerPrerendered" />
             
            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <div class="alert alert-danger mt-3">@Model.ErrorMessage</div>
            }
        </div>
    </div>
</div>
@page "/Reparti/AddReparto"
@model VirtualChief.Pages.Reparti.AddRepartoModel
@{
    ViewData["Title"] = "Aggiungi Reparto";
}

<div class="container">
    <nav aria-label="breadcrumb" class="mt-3 mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="/Reparti/listReparti">@Model.NavReparti</a>
            </li>
            <li class="breadcrumb-item active" aria-current="page">@Model.NavAddReparto</li>
        </ol>
    </nav>

    <h1>@Model.NavAddReparto</h1>

    <div class="card">
        <div class="card-body">
            <component type="typeof(Components.Reparti.AddReparto)" 
                       param-Tenant="@Model.Tenant"
                       param-HasPermission="@Model.HasPermission"
                       render-mode="ServerPrerendered" />
             
            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <div class="alert alert-danger mt-3">@Model.ErrorMessage</div>
            }
        </div>
    </div>
</div>
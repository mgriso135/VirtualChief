@page "/Produzione/ListStatusUtentiReparto"
@model VirtualChief.Pages.Produzione.ListStatusUtentiRepartoModel
@{
    ViewData["Title"] = "Stato Utenti Reparto";
}

<div class="container">
    <nav aria-label="breadcrumb" class="mt-3 mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="/Produzione/produzione">@Model.NavProduzione</a>
            </li>
            <li class="breadcrumb-item active" aria-current="page">@Model.NavStatusUtentiReparto</li>
        </ol>
    </nav>

    <h1>@Model.NavStatusUtentiReparto</h1>

    <div class="card">
        <div class="card-body">
            <component type="typeof(Components.Produzione.ListStatusUtentiReparto)" 
                       param-Tenant="@Model.Tenant"
                       param-HasPermission="@Model.HasPermission"
                       param-RepId="@Model.RepId"
                       render-mode="ServerPrerendered" />
             
            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <div class="alert alert-danger mt-3">@Model.ErrorMessage</div>
            }
        </div>
    </div>
</div>
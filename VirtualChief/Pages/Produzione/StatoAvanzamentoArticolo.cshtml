@page "/Produzione/StatoAvanzamentoArticolo"
@model VirtualChief.Pages.Produzione.StatoAvanzamentoArticoloModel
@{
    ViewData["Title"] = "Stato Avanzamento Articolo";
}

<div class="container">
    <nav aria-label="breadcrumb" class="mt-3 mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="/Produzione/produzione">@Model.NavProduzione</a>
            </li>
            <li class="breadcrumb-item active" aria-current="page">@Model.NavStatoAvanzamento</li>
        </ol>
    </nav>

    <h1>@Model.NavStatoAvanzamento</h1>

    <div class="card">
        <div class="card-body">
            <component type="typeof(Components.Produzione.StatoAvanzamentoArticolo)" 
                       param-Tenant="@Model.Tenant"
                       param-HasPermission="@Model.HasPermission"
                       param-ArtId="@Model.ArtId"
                       param-ArtYear="@Model.ArtYear"
                       render-mode="ServerPrerendered" />
             
            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <div class="alert alert-danger mt-3">@Model.ErrorMessage</div>
            }
        </div>
    </div>
</div>